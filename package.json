{
  "name": "milletsnow-qr-feedback-system",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "node scripts/dev.mjs",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "vercel-build": "npm --prefix backend install && npm --prefix backend run build && npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm run build && npm --prefix backend run start"
  }
}
