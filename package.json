{
  "name": "milletsnow-qr-feedback-system",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install --workspaces --include-workspace-root",
    "dev": "node scripts/dev.mjs",
    "vite": "npm --workspace backend run build && npm --workspace frontend run build",
    "build": "npm --workspace backend run build && npm --workspace frontend run build",
    "vercel-build": "npm --workspace backend exec -- prisma generate --schema src/prisma/schema.prisma && npm --workspace backend exec -- tsc -p tsconfig.vercel.json && npm --workspace frontend run build",
    "start": "npm run build && npm --workspace backend run start"
  }
}
